RewriteEngine On

# If file/folder exists, serve it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise route to React
RewriteRule ^ index.html [L]
